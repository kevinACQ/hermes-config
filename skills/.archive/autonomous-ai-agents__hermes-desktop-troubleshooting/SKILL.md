---
name: hermes-desktop-troubleshooting
description: 'Diagnose Hermes Desktop app issues where the UI, gateway runtime, and stored session database disagree — e.g. Desktop says "session not found" even though `hermes sessions list` shows it.'
version: 0.0.0-archive-reconstructed
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [archive, reconstructed, consolidated]
    absorbed_into: hermes-agent
---

# hermes-desktop-troubleshooting

This archived package is a reconstructed recovery placeholder created during an umbrella consolidation pass after the active narrow skill was absorbed into `hermes-agent`.

The reusable class-level instructions now live in `hermes-agent` under **Consolidated Hermes Operations**. This archive preserves the skill identity and known trigger/linked-file shape so the archive remains recoverable and downstream migration tooling can identify that the skill was consolidated rather than pruned.

Known linked file from the original package: `references/desktop-session-not-found.md`.
