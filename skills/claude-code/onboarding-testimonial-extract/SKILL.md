---
name: onboarding-testimonial-extract
description: Mine ACQ Vantage onboarding call recordings (Fathom) for the most clip-worthy testimonial moments and assemble per-member "Extract" sheets for Kevin's highlight reel. Use whenever Kevin wants testimonial clips, a highlight/sizzle reel, video-ad quotes, or the "best moments" / "what clients said" from onboarding or client calls — even when phrased as "find good clips from my calls", "pull testimonials", "what did clients say about the onboarding", or "extract quotes from <member>'s call". Ranks by the customer's own benefit/problem-solved language first (the 10/10 score is only a proxy that the call went well). Reads the Fathom call-log Sheet read-only, scans transcripts via parallel subagents, presents a ranked shortlist for Kevin to pick from, then outputs verbatim quotes with click-to-moment Fathom deep links into a Google Doc.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [claude-code, wrapper, slash-command]
    related_skills: [claude-code-skills]
---

# Onboarding Testimonial Extract Claude Code Skill Wrapper

## Purpose

This is a Hermes-native slash-command wrapper for Kevin's Claude Code skill `onboarding-testimonial-extract`.
Claude remains the source of truth. This wrapper exists so Kevin can invoke the workflow in Hermes as:

```text
/onboarding-testimonial-extract <task or target>
```

## Required behavior

When this skill is invoked:

1. Immediately read the source Claude skill:
   - `/Users/kevin/.hermes/external-repos/claude-code-skills/onboarding-testimonial-extract/SKILL.md`
2. Treat that Claude `SKILL.md` as the authoritative workflow for this task.
3. Follow the Claude skill exactly where compatible with Hermes tools and safety boundaries.
4. Preserve any user instruction supplied after `/onboarding-testimonial-extract` as the task target.
5. If the Claude skill references supporting files under `references/`, `templates/`, `scripts/`, or `assets/`, resolve them relative to:
   - `/Users/kevin/.hermes/external-repos/claude-code-skills/onboarding-testimonial-extract`
6. Do not write to `/Users/kevin/.claude` unless Kevin explicitly asks. If the Claude skill includes memory/session-writing behavior, reinterpret it into Hermes-native memory or Hermes-owned files unless Kevin explicitly asks to update Claude state.

## Source of truth

- Claude skill directory: `/Users/kevin/.hermes/external-repos/claude-code-skills/onboarding-testimonial-extract`
- This wrapper is generated by: `/Users/kevin/.hermes/scripts/generate_claude_skill_wrappers.py`

## Verification

Before acting on the task, confirm the source skill was read successfully. If it cannot be read, stop and report the exact missing path.
