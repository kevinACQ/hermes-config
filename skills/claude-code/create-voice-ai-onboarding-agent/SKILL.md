---
name: create-voice-ai-onboarding-agent
description: Recreates the ACQ Vantage Voice AI Onboarding Agent from scratch by loading the SOP, executing each setup step via ElevenLabs/Twilio/Google APIs, and verifying the full end-to-end flow. Invoke when user says "set up the voice AI onboarding agent", "recreate the onboarding agent", "rebuild the voice onboarding system", "restore the onboarding agent to v0.9.1", or "configure the ElevenLabs agent from scratch". Do NOT invoke for individual prompt tweaks or TTS tuning (PATCH the agent directly); do NOT invoke for transcript analysis (use voice-language-pattern-extraction). Always load the SOP before executing any API call — the SOP is the source of truth for all values.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [claude-code, wrapper, slash-command]
    related_skills: [claude-code-skills]
---

# Create Voice Ai Onboarding Agent Claude Code Skill Wrapper

## Purpose

This is a Hermes-native slash-command wrapper for Kevin's Claude Code skill `create-voice-ai-onboarding-agent`.
Claude remains the source of truth. This wrapper exists so Kevin can invoke the workflow in Hermes as:

```text
/create-voice-ai-onboarding-agent <task or target>
```

## Required behavior

When this skill is invoked:

1. Immediately read the source Claude skill:
   - `/Users/kevin/.hermes/external-repos/claude-code-skills/create-voice-ai-onboarding-agent/SKILL.md`
2. Treat that Claude `SKILL.md` as the authoritative workflow for this task.
3. Follow the Claude skill exactly where compatible with Hermes tools and safety boundaries.
4. Preserve any user instruction supplied after `/create-voice-ai-onboarding-agent` as the task target.
5. If the Claude skill references supporting files under `references/`, `templates/`, `scripts/`, or `assets/`, resolve them relative to:
   - `/Users/kevin/.hermes/external-repos/claude-code-skills/create-voice-ai-onboarding-agent`
6. Do not write to `/Users/kevin/.claude` unless Kevin explicitly asks. If the Claude skill includes memory/session-writing behavior, reinterpret it into Hermes-native memory or Hermes-owned files unless Kevin explicitly asks to update Claude state.

## Source of truth

- Claude skill directory: `/Users/kevin/.hermes/external-repos/claude-code-skills/create-voice-ai-onboarding-agent`
- This wrapper is generated by: `/Users/kevin/.hermes/scripts/generate_claude_skill_wrappers.py`

## Verification

Before acting on the task, confirm the source skill was read successfully. If it cannot be read, stop and report the exact missing path.
